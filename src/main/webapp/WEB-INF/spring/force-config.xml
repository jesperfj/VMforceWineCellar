<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:security="http://www.springframework.org/schema/security"
       xmlns:fss="http://www.salesforce.com/schema/springsecurity"
	   xsi:schemaLocation="
        http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context-3.0.xsd
        http://www.springframework.org/schema/tx
        http://www.springframework.org/schema/tx/spring-tx-3.0.xsd
        http://www.springframework.org/schema/security
        http://www.springframework.org/schema/security/spring-security-3.0.xsd
        http://www.salesforce.com/schema/springsecurity
        http://www.salesforce.com/schema/springsecurity/force-springsecurity-1.0.xsd">
        

    <context:component-scan base-package="com.vmforce.samples.winecellar" />

    <tx:annotation-driven />
    

	<context:property-placeholder location="classpath:force.properties" />
		
    <!-- Drives transactions using local JPA APIs -->
    <bean id="transactionManager" class="org.springframework.orm.jpa.JpaTransactionManager">
        <property name="entityManagerFactory" ref="entityManagerFactory" />
    </bean>
    
    <bean id="entityManagerFactory" class="org.springframework.orm.jpa.LocalEntityManagerFactoryBean">
        <property name="persistenceUnitName" value="forceDatabase" />
    </bean>

    <bean class="org.springframework.orm.jpa.support.PersistenceAnnotationBeanPostProcessor" />

 	<bean id="forceService" class="com.force.sdk.connector.ForceServiceConnector">
 		<property name="username" value="${force.userName}"/>
 		<property name="password" value="${force.password}"/>
 		<property name="endPoint" value="${force.endPoint}/services/Soap/u/20.0"/>
    </bean>

	<!-- SFDC OAuth security config -->
	
    <fss:oauth end-point="${force.endPoint}" consumer-key="${force.consumerKey}"
        consumer-secret="${force.consumerSecret}" logout-from-sfdc="true" />

    <!-- Configure Spring Security -->

    <security:http>
        <security:anonymous />
	    <security:intercept-url pattern="/**" access="ROLE_USER" />
    </security:http>

</beans>
